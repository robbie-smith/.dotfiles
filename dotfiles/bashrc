export BASH_SILENCE_DEPRECATION_WARNING=1

# Resolve the repo root by following this file's symlink, rather than assuming
# ~/.dotfiles -- the repo does not have to be cloned there. This was hardcoded,
# so a clone at any other path made the sources below fail on every new shell.
# It also means moving the repo only requires re-pointing the ~ symlinks.
DOTFILES_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/.." && pwd)"

export EDITOR=nvim
export PATH=$HOME/.local/bin:$PATH
export PATH="$HOME/.cargo/bin:$PATH"
export PATH="/usr/local/Cellar:$PATH"
export PATH="/usr/local/bin:$PATH"
export PATH="/usr/local/opt/postgresql@9.5/bin:$PATH"
export PATH=/Applications/SnowSQL.app/Contents/MacOS:$PATH
export PATH=/Applications/Postgres.app/Contents/Versions/12/bin:$PATH
export GOPATH=$HOME/Go
export PATH=$PATH:$GOPATH/bin
export PYTEST_ADDOPTS='-s'
export CLICOLOR=1
export TERM=xterm-256color
export LSCOLORS=GxFxCxDxBxegedabagaced
export CLICOLOR=1
export TERM=xterm-256color
export LSCOLORS=GxFxCxDxBxegedabagaced
# SCALA_HOME removed -- pointed at /usr/local/opt/scala/idea (Intel path, never
# existed on this machine) and scala was not installed. mise owns scala now.
export PYENV_ROOT="$HOME/.pyenv"
export PATH="$PYENV_ROOT/bin:$PATH"
bind 'set show-all-if-ambiguous on'
bind 'set completion-ignore-case on'
bind 'TAB:menu-complete'
export HISTCONTROL=ignoreboth
source "$DOTFILES_DIR/bash_base_config/fzf_profile.sh"
source "$DOTFILES_DIR/bash_base_config/git_prompt.sh"
source "$DOTFILES_DIR/bash_base_config/prompt.sh"
source "$DOTFILES_DIR/bash_base_config/aliases.sh"
# export LDFLAGS="-L/usr/local/opt/libpq/lib"
export LDFLAGS="-L/opt/homebrew/opt/libpq/lib"
export CPPFLAGS="-I/usr/local/opt/libpq/include"
export PATH="/usr/local/opt/libpq/bin:$PATH"
export PKG_CONFIG_PATH="/usr/local/opt/libpq/lib/pkgconfig"
# HOMEBREW_CELLAR / SPARK_HOME removed. HOMEBREW_CELLAR was hardcoded to the
# Intel path and then silently corrected by `brew shellenv` further down -- but
# too late for SPARK_HOME, which had already expanded to a directory that does
# not exist. apache-spark is not installed via brew either.
export DOCKER_CLI_HINTS=false
export PATH=$PATH:$HOME/.toolbox/bin
export PATH="/usr/local/sbin:$PATH"
export AWS_PROFILE="foundry24"
export GITHUB_TOKEN="$(security find-generic-password -a "$USER" -s claude-github-token -w 2>/dev/null)"
if command -v pyenv 1>/dev/null 2>&1; then
  eval "$(pyenv init -)"
fi
test -e "${HOME}/.iterm2_shell_integration.bash" && source "${HOME}/.iterm2_shell_integration.bash"


# nvm removed -- node is managed by mise (see mise_setup_node in setup.sh).
# ~/.nvm never existed on this machine, so this block was already a no-op.

if [ -f /sw/etc/bash_completion ]; then
   . /sw/etc/bash_completion
fi
. "$HOME/.cargo/env"
eval "$(/opt/homebrew/bin/brew shellenv)"

[ -f ~/.fzf.bash ] && source ~/.fzf.bash

# Work (Garner) config -- devn, Therapi, GitLab/uv. Loaded only on the work
# machine, keyed off devn's presence. See bash_base_config/work.sh.
[[ -f "$HOME/.config/devn/devn_shell_functions.sh" ]] && \
  source "$DOTFILES_DIR/bash_base_config/work.sh"
